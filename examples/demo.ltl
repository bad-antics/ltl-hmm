// examples/demo.ltl — runnable demo for ltl_hmm

import std::io
use ltl_hmm as lib

fn main() {
    io::println("ltl_hmm demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}